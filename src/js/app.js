import { /* bootstrapMenu, */bootstrapToggleMenu } from './_menu';
import { bootstrapTool } from './_tool';
import { bootstrapRouter } from './_router';

window.addEventListener('DOMContentLoaded', () => {
  // bootstrapMenu();
  bootstrapToggleMenu();
  bootstrapTool();
  bootstrapRouter();
});
